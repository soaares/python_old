# Desafio 69

>>> idade = 12
>>> idade < 15
True
>>> idade > 15
False
>>> idade >= 15
False
>>> idade <= 15
True
>>> idade <= 12
True

# No primeiro exemplo, o valor retornado é True pois o valor definido na
# variável é menor que o segundo valor fornecido.
# No segundo exemplo pois o valor definido na variável não é maior que o 
# segundo valor fornecido.
# No terceiro exemplo, o valor retornado é False pois o valor definido na
# variável não é maior que o segundo valor e nem igual.
# No quarto exemplo, o valor retornado é True pois o valor da variável é
# menor que o segundo valor fornecido.
# No quinto exemplo o valor retornado é True, o valor da variável não é
# menor que 12, porém, é igual. Para que o valor True seja retornado nesse
# caso, uma das duas condições deve ser satisfeita. 